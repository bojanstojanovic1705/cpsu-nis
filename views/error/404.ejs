<!DOCTYPE html>
<html lang="sr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stranica nije pronađena | CPSU</title>
    
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        body {
            min-height: 100vh;
            display: flex;
            align-items: center;
            background-color: #f8f9fa;
            overflow: hidden;
        }
        .error-container {
            text-align: center;
            position: relative;
        }
        .error-number {
            font-size: 150px;
            font-weight: bold;
            color: #0d6efd;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
            margin: 0;
            animation: bounce 2s infinite;
        }
        .error-text {
            font-size: 24px;
            color: #6c757d;
            margin: 20px 0;
        }
        .home-button {
            display: inline-block;
            padding: 15px 30px;
            background-color: #0d6efd;
            color: white;
            text-decoration: none;
            border-radius: 50px;
            transition: transform 0.3s, box-shadow 0.3s;
            animation: float 3s infinite;
        }
        .home-button:hover {
            background-color: #0b5ed7;
            color: white;
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }
        .floating-icons {
            position: absolute;
            width: 100%;
            height: 100%;
            pointer-events: none;
        }
        .floating-icon {
            position: absolute;
            font-size: 30px;
            color: #0d6efd;
            opacity: 0.3;
            animation: float-icon 6s infinite;
        }
        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }
            40% {
                transform: translateY(-30px);
            }
            60% {
                transform: translateY(-15px);
            }
        }
        @keyframes float {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-10px);
            }
        }
        @keyframes float-icon {
            0% {
                transform: translate(0, 0) rotate(0deg);
            }
            50% {
                transform: translate(20px, -20px) rotate(180deg);
            }
            100% {
                transform: translate(0, 0) rotate(360deg);
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="error-container">
            <!-- Floating Icons -->
            <div class="floating-icons">
                <i class="fas fa-home floating-icon" style="top: 10%; left: 10%;"></i>
                <i class="fas fa-heart floating-icon" style="top: 20%; right: 20%;"></i>
                <i class="fas fa-star floating-icon" style="bottom: 30%; left: 30%;"></i>
                <i class="fas fa-smile floating-icon" style="bottom: 20%; right: 10%;"></i>
            </div>
            
            <!-- Main Content -->
            <h1 class="error-number">404</h1>
            <p class="error-text mb-4">Ups! Stranica koju tražite nije pronađena.</p>
            <p class="mb-5">Ne brinite, možete se vratiti na početnu stranicu.</p>
            <a href="/" class="home-button">
                <i class="fas fa-home me-2"></i>Početna stranica
            </a>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Custom Animation Script -->
    <script>
        // Dodatne animacije za plutajuće ikone
        document.querySelectorAll('.floating-icon').forEach(icon => {
            icon.style.animationDelay = Math.random() * 2 + 's';
        });
    </script>
</body>
</html>
